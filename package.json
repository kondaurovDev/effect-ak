{
  "private": "true",
  "name": "monorepo",
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "test": "vitest run",
    "compile": "tsc -b tsconfig.src.json",
    "pack-all": "pnpm -r pack-tar"
  },
  "devDependencies": {
    "@types/node": "20.14.8",
    "glob": "^11.0.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
